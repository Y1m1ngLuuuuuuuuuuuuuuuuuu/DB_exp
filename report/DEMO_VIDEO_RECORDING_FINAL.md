# 学生选课成绩管理系统录屏最终版脚本

## 录制定位

视频建议控制在 8 到 10 分钟。展示重点不是单纯浏览页面，而是说明系统如何把数据库原理2中的设计流程、ER 建模、关系模型、规范化、完整性约束、触发器、事务、锁、视图和运行维护落实到一个学生选课成绩管理系统中。

录屏过程中只展示页面、报告、测试输出和必要 SQL 片段，不展示明文密码文件，不现场修改数据库结构，不把后续扩展讲成已实现。

## 演示前准备

1. 启动 openGauss 容器，并确认数据库已经完成初始化、完整性增强和绩点规则脚本。
2. 启动 Streamlit：

```bash
.venv/bin/streamlit run app.py --server.port 8501 --server.address 127.0.0.1
```

3. 准备最终 PDF 报告。
4. 准备演示账号：管理员使用 `A001`，教师可使用 `T005` 或 `T021`，学生可使用 `20240004`、`20240008` 或 `20240012`。
5. 明文密码只从本地凭据文件中查看，录屏画面不展示该文件。

## 时间轴与口播稿

| 时间 | 画面 / 操作 | 口播重点 | 数据库知识点 |
| --- | --- | --- | --- |
| 0:00--0:30 | 打开登录页或报告封面 | “本项目是基于 openGauss 的学生选课成绩管理系统，展示重点是数据库设计与一致性维护，而不是普通页面功能。” | 系统规划 |
| 0:30--1:10 | 登录页，展示三类账号规则 | “系统使用统一账户表保存认证信息，学生、教师、管理员档案分表保存。角色档案一致性由触发器检查。” | 账户模型、权限、逻辑设计 |
| 1:10--2:00 | 管理员首页 | “管理员首页展示学生、教师、课程、教学班、选课记录和审计日志数量。演示数据覆盖 4 个学院、100 名学生、30 名教师和 50 门课程。” | 需求分析、数据规模 |
| 2:00--3:00 | PDF 中的 ER 图与逻辑模型图 | “概念模型抽取院系、专业、学生、教师、课程、学期、教室、教学班、选课记录和成绩日志等实体，并通过关系模型转换为表结构。” | ER 图、关系模型 |
| 3:00--3:50 | 课程或教学班管理页面 | “课程定义和具体教学班分离。`course` 保存课程名称、类型、学分等定义，`course_offering` 保存学期、教师、教室和容量，`course_schedule` 保存结构化上课时间片。” | 3NF、实体分解 |
| 3:50--5:00 | 学生课程查询与选课 | “学生与教学班之间是多对多关系，系统用 `enrollment` 保存选课记录。数据库层同时检查容量、重复选课、同课跨班、时间冲突和先修课程。” | M:N 转换、触发器、唯一约束 |
| 5:00--5:50 | 展示一个选课失败提示，或用报告/测试结果说明 | “这些错误不是只靠前端判断，而是由数据库触发器和约束兜底。即使绕过页面直接写库，也会被数据库拒绝。” | CHECK、UNIQUE、跨表完整性 |
| 5:50--6:40 | 学生首页、课表或成绩页面 | “课表由结构化时间片和视图支撑。绩点不保存在 `enrollment` 基础表中，而是通过 `grade_policy`、`grade_scale` 和视图计算，避免派生字段不一致。” | 视图、派生数据处理 |
| 6:40--7:40 | 教师端教学班和成绩录入 | “教师录入成绩时，服务层在事务中设置操作者和原因，数据库触发器自动写入成绩修改日志。” | 事务、审计触发器 |
| 7:40--8:20 | 管理员审计日志或报告中的日志表说明 | “审计日志保存旧成绩、新成绩、修改人、修改时间和原因，支持运行维护和责任追踪。” | 数据库审计 |
| 8:20--9:10 | 展示并发测试输出或报告第 7 章 | “两个独立连接同时抢容量为 1 的教学班，结果只有一个成功。`SELECT ... FOR UPDATE` 锁定教学班行，使容量检查串行化。” | 事务隔离、行级锁 |
| 9:10--10:00 | PDF 中范式、触发器、事务章节和总结页 | “报告按照数据库设计流程组织，核心内容覆盖 ER、关系模型、范式、完整性、触发器、事务锁、索引、视图和运行维护。” | 课程知识点总结 |

## 详细操作顺序

### 1. 登录与角色体系

1. 打开系统登录页。
2. 指出账号规则：学生使用学号，教师使用教师编号，管理员使用管理员编号。
3. 输入管理员账号 `A001` 登录。

讲解句：

> 系统没有把所有用户信息混在一张表中，而是用 `user_account` 保存认证信息，再用 `student`、`teacher`、`admin_profile` 保存不同角色档案。数据库触发器负责检查账号角色和档案类型是否一致。

### 2. 管理员首页与数据规模

1. 展示管理员首页指标卡。
2. 指出学生、教师、课程、教学班、选课记录和审计日志数量。

讲解句：

> 这些统计结果来自基础表和视图，说明系统已经不是少量样例数据，而是有一定规模的演示数据，可以支撑查询、统计和审计展示。

### 3. ER 图和逻辑模型

1. 切换到 PDF 报告。
2. 展示概念 ER 图。
3. 展示逻辑关系模型或第 4 章关系模式定义。

讲解句：

> 概念设计阶段关注实体和联系，逻辑设计阶段把它们转换成关系模式。学生和教学班之间的多对多关系通过 `enrollment` 转换；课程和先修课程之间的自关联通过 `course_prerequisite` 转换。

### 4. 课程、教学班和时间片

1. 在管理员端展示课程或教学班管理页面。
2. 指出课程、教师、学期、教室、容量、上课时间。

讲解句：

> 课程定义与教学班分离可以避免课程名称、学分、教师、教室和学期信息在同一张大表中重复出现。上课时间用 `course_schedule` 结构化保存，便于数据库执行时间冲突检查。

### 5. 学生选课

1. 退出管理员账号，登录学生账号。
2. 打开课程查询与选课页面。
3. 筛选开放学期，选择一门有余量、无冲突的课程。
4. 点击选课并展示成功提示。
5. 打开课表确认课程出现。

讲解句：

> 选课写入不是简单 INSERT，而是由服务层事务调用数据库函数。数据库层锁定目标教学班，统计已选人数，并通过触发器检查容量、时间冲突、先修课程和同课跨班限制。

### 6. 选课错误案例

建议只展示一个或两个，避免视频过长：

- 重复选同一教学班；
- 同一学期重复选择同一课程的不同教学班；
- 时间冲突；
- 容量已满；
- 先修课程条件不满足。

讲解句：

> 这些错误由数据库约束或触发器兜底。前端会给出友好提示，但真正保证一致性的是数据库层规则。

### 7. 学生成绩与绩点

1. 打开学生成绩页面。
2. 展示课程、学分、成绩、状态和绩点。

讲解句：

> 系统不在 `enrollment` 中保存 `gpa_point` 派生字段，而是通过绩点规则表和视图计算。这样成绩变化或绩点规则变化时，不会出现基础表中成绩和绩点不一致的问题。

### 8. 教师成绩录入与审计

1. 登录教师账号。
2. 打开教学班或成绩录入页面。
3. 选择一个学生，修改成绩并填写原因。
4. 保存后展示成功提示。

讲解句：

> 成绩更新和审计日志写入处于同一事务中。服务层设置当前操作者，数据库触发器写入 `score_change_log`。如果日志写入失败，成绩更新也会回滚。

### 9. 管理员审计日志

1. 回到管理员账号。
2. 打开成绩审计日志页面。
3. 展示旧成绩、新成绩、修改人、修改时间和原因。

讲解句：

> 审计日志不是普通冗余字段，而是独立事件记录。它保留了成绩修改历史，支持后续追责和运行维护。

### 10. 并发抢课测试

1. 展示并发测试脚本输出或报告附录。
2. 重点展示：

```text
success_count = 1
failed_count = 1
selected_count_in_db = 1
```

讲解句：

> 两个数据库连接同时抢一个容量为 1 的教学班，最终只有一个成功。核心原因是 `SELECT ... FOR UPDATE` 对同一个教学班行加锁，使容量检查和写入按事务顺序执行。

## 收尾总结

可以用下面这段作为结尾：

> 总体来看，本系统把选课业务建模为一个规范化的关系数据库应用。报告从规划、需求、概念、逻辑、规范化、完整性、事务锁、物理设计到运行维护展开，重点体现数据库原理2中的 ER 模型、关系模型、范式、触发器、事务、锁、索引和视图。后续可以继续完善生产级权限、备份恢复、运行监控、教师评价和更大规模并发压测。

## 录制注意事项

1. 不展示明文密码文件。
2. 不展示 `secrets` 目录。
3. 不现场修改数据库结构。
4. 不把教师评价讲成已实现功能。
5. 不把 SQL 文件全文逐行讲解。
6. 页面报错时只展示友好提示，不展示数据库异常堆栈。
7. 若演示选课错误案例，提前选好测试课程，避免现场找不到合适数据。
