\chapter{总结}

\section{系统设计总结}

本项目围绕高校选课业务建立了较完整的关系数据库模型。系统区分课程定义和开课班次，使用 \code{enrollment} 转换学生与教学班之间的多对多联系，使用 \code{course_schedule} 结构化表达上课时间片，并通过 \code{course_prerequisite} 表示课程先修自关联。数据库结构由原生 SQL 流程维护，并通过 \code{grade_policy} 与 \code{grade_scale} 支持绩点规则版本化。

演示数据覆盖 4 个学院、11 个专业、100 名学生、30 名教师、50 门课程、175 个教学班、1480 条选课记录和 80 条成绩审计日志。该数据规模能够支撑学生选课、课表展示、成绩单、教师成绩录入、管理员统计和成绩审计等页面演示，也为容量、时间冲突、同课跨班、先修课和视图统计提供了更接近真实业务的验证样本。

\section{课程知识点体现}

项目较好体现了数据库原理2的多个重点内容：

\begin{enumerate}
  \item 数据库设计流程：报告按规划、需求、概念、逻辑、规范化、物理和运行维护展开。
  \item ER 建模：实体、属性、联系和基数均能从真实表结构中抽取。
  \item 关系模型：M:N 联系通过 \code{enrollment} 和 \code{course_prerequisite} 转换为关系模式。
  \item 规范化：在数据库原理1系统设计基础上，将上课时间结构化，并将已选人数、绩点等可计算信息放在视图或规则表查询层处理，使核心基础表更接近 3NF。
  \item 完整性：主键、外键、唯一约束、CHECK、DEFAULT 和触发器共同维护数据正确性。
  \item 事务与锁：选课流程使用 \code{select_course_tx()}、\code{DBSession} 和 \code{SELECT ... FOR UPDATE} 控制并发。
  \item 物理设计：项目为会话、教学班、时间片、选课记录和成绩日志建立了常用索引，并用视图提升查询便利性。
  \item 测试验证：回滚型测试覆盖容量、时间冲突、重复选课、先修关系、成绩审计和视图查询等数据库规则。
\end{enumerate}

% \section{项目不足}

% 不足主要集中在以下四个方面：

% \begin{enumerate}
%   \item 数据库级最小权限已经提供角色授权脚本和本地演示账号绑定脚本，但正式部署时仍需要绑定真实数据库账号，并接入环境变量或密钥管理系统。
%   \item 多连接高并发压力测试仍可扩展。系统已经提供容量为 1 的并发抢课脚本，但还可以继续模拟更多学生、更多热门教学班和更长锁等待场景。
%   \item 备份恢复、运行监控和物化视图仍是后续工作，统计报表在数据量很大时还需进一步评估。
%   \item 课程结束后的教师评价打分模块尚未作为主线功能实现，目前仅作为后续扩展设计保留。
% \end{enumerate}

% \section{后续改进}

% 如果继续完善项目，优先级较高的数据库相关功能包括：

% \begin{enumerate}
%   \item 将本地演示的数据库账号绑定方案迁移到真实生产账号，并使用密钥管理系统保管数据库登录密码。
%   \item 增加更大规模并发压测，验证最后一个名额抢课时的锁等待、异常返回和事务回滚行为。
%   \item 建立更完整的运行维护方案，包括备份恢复、监控告警、SQL 脚本版本管理和必要时的物化视图刷新策略。
%   \item 将教师评价作为后续扩展模块设计，建议使用 \code{teaching_evaluation(evaluation_id, enrollment_id, rating, comment, submitted_at)}，并通过 \code{UNIQUE(enrollment_id)}、\code{CHECK(rating BETWEEN 1 AND 5)} 以及课程完成状态检查保证评价数据质量。
% \end{enumerate}

% 总体而言，本项目已经不是简单的页面 CRUD 程序，而是在数据库原理1系统功能基础上进一步体现数据库设计流程、范式理论、完整性约束、事务锁机制和运行维护思路的数据库应用系统。报告中的不足部分也说明数据库设计需要随着业务规则和运行环境持续优化。
